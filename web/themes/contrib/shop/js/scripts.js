/**
 * @file
 * Behaviors for the shop theme.
 */

(function ($, _, Drupal) {
  Drupal.behaviors.shop = {
    attach() {
      // shop JavaScript behaviors goes here.
    },
  };
})(window.jQuery, window._, window.Drupal);
